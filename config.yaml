suche:
  stadt: "Weinfelden"
  radius_km: 15
  preis_max: 1000
  preis_min: 200   # filtert z.B. Garagen/Parkplätze raus; null = keine Mindestgrenze
  zimmer_min: null   # null = keine Mindestanforderung
email:
  empfaenger: "icurafu333@gmail.com"   # Fallback, wenn RECIPIENT_EMAIL in .env nicht gesetzt ist
  nur_bei_treffern: true   # false = auch Emails ohne neue Treffer verschicken
scraper:
  aktiviert: [flatfox, homegate, immoscout24, comparis]
  rate_limit_sekunden: 2
  cooldown_stunden: 3   # nach einem Fehler wird dieses Portal für X Stunden übersprungen
